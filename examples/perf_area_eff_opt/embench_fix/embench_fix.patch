diff --git a/embench/baremetal_init.c b/embench/baremetal_init.c
index 80e68e5..d288178 100644
--- a/embench/baremetal_init.c
+++ b/embench/baremetal_init.c
@@ -5,6 +5,9 @@
 extern volatile uint64_t tohost;
 extern volatile uint64_t fromhost;
 
+// declaring main first, otherwise GCC treats main() calling as an error.
+int main(int argc, char **argv);
+
 void __attribute__((noreturn)) tohost_exit(uintptr_t code)
 {
     tohost = (code << 1) | 1;
